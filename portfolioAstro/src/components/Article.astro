---
interface Props {
  title: string;
  className?: string;
}

const { title, className } = Astro.props;
---

<section class:list={[className, "u-mb-24"]}>
  <div class="subtitle-wrapper">
    <h4 class="section-title">{title}</h4>
  </div>
  <div class="section-paragraph">
    <slot />
  </div>
</section>
